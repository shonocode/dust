# Free API key from https://api.data.gov/signup/ for the crime stats page.
# Falls back to DEMO_KEY (~10 requests/hour) when unset.
# Note: as a client-side SPA, this key is visible in the shipped bundle.
VITE_DATA_GOV_API_KEY=
